# The bundled Empirica archive is a build artifact (regenerated by
# `infra/scripts/build-nas-bundle.sh`). It is large and binary, and
# including it in git would make every clone carry the previous
# bundle. Keep it out of the index. The Dockerfile expects it to
# exist on disk at build time, which is the build script's job.
delibra.tar.zst
