{
  "dependencies": {
    "@empirica/core": "1.12.0",
    "ajv": "^8.20.0",
    "dotenv": "^16.4.5",
    "lodash": "^4.17.21",
    "minimist": "1.2.6",
    "openai": "^4.47.3",
    "playwright": "^1.62.1"
  },
  "devDependencies": {
    "esbuild": "0.25.0"
  },
  "name": "empirica-server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist/prompts && cp src/HPTConfig.json dist/HPTConfig.json && cp -R src/prompts/. dist/prompts/ && esbuild src/index.js --color=true --log-level=warning --platform=node --define:process.env.NODE_ENV='\"production\"' --banner:js='import { createRequire } from \"module\"; const require = createRequire(import.meta.url);' --bundle --minify --outfile=dist/index.js --sourcemap",
    "test:real-adaptive": "node real-adaptive-evaluation.mjs",
    "dev": "npm run build && node --trace-warnings --enable-source-maps --unhandled-rejections=warn-with-error-code dist/index.js",
    "serve": "node --trace-warnings --enable-source-maps --unhandled-rejections=warn-with-error-code index.js"
  },
  "volta": {
    "node": "20.11.1",
    "npm": "10.2.4"
  },
  "overrides": {
    "minimatch": "^10.2.1"
  }
}
